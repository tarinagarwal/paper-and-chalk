# Paper & Chalk environment. Copy to .env (cp .env.example .env) and fill in the blanks.
# Every app validates its own variables at startup and refuses to start if one is missing.

# --- Docker Compose host ports (optional; defaults shown) ---------------------------------------
# PC_MONGO_PORT=27027

# --- MongoDB (used by: web, db scripts) ---------------------------------------------------------
# Development uses the Atlas cluster. Tests use the local container (MONGODB_TEST_URI).
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/PaperDB?appName=Cluster0
MONGODB_TEST_URI=mongodb://localhost:27027/paper_chalk_test?directConnection=true

# --- Upstash Redis (used by: web rate limiting and health) --------------------------------------
UPSTASH_REDIS_REST_URL=https://<your-db>.upstash.io
UPSTASH_REDIS_REST_TOKEN=<upstash-rest-token>
# "upstash" (default) or "memory" (tests and CI only; refused in production).
RATE_LIMIT_STORE=upstash
# Native Redis protocol (TLS), for the sync server's Redis extension later.
REDIS_URL=rediss://default:<upstash-token>@<your-db>.upstash.io:6379

# --- Auth (used by: web) ------------------------------------------------------------------------
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
BETTER_AUTH_SECRET=<32+ random bytes, base64url>
BETTER_AUTH_URL=http://localhost:3000
# Google OAuth client (optional in development: the Google button hides when unset).
# Redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- Email (used by: web) -----------------------------------------------------------------------
# "console" prints magic links to the terminal, "outbox" writes them to .data/outbox,
# "smtp" sends real email. Production requires "smtp".
EMAIL_DELIVERY=console
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=<you@gmail.com>
SMTP_PASSWORD=<gmail-app-password>
EMAIL_FROM="Paper & Chalk <you@gmail.com>"

# --- Sync identity (used by: web signs, sync verifies) ------------------------------------------
SYNC_JWT_SECRET=<32+ random bytes, base64url>

# --- AWS S3 (used by: web, workers, storage scripts) ---------------------------------------------
# Five private buckets (section 2). Dev, tests and CI all use the dev buckets; tests and CI write
# under S3_KEY_PREFIX=test/, which a lifecycle rule deletes after a day.
S3_REGION=ap-south-1
S3_BUCKET_ORIGINALS=paper-chalk-dev-originals
S3_BUCKET_YJS_SNAPSHOTS=paper-chalk-dev-yjs-snapshots
S3_BUCKET_ASSETS=paper-chalk-dev-assets
S3_BUCKET_EXPORTS=paper-chalk-dev-exports
S3_BUCKET_THUMBNAILS=paper-chalk-dev-thumbnails
# The app's IAM user (read/write objects in these buckets only). Leave both unset to use the AWS
# default chain (AWS_* variables, a role, or the CLI profile).
S3_ACCESS_KEY_ID=<app IAM user access key>
S3_SECRET_ACCESS_KEY=<app IAM user secret>
# Prepended to every new object key. Empty for dev; tests and CI use test/.
S3_KEY_PREFIX=
# Origins allowed to upload and read from the browser. Used by `pnpm storage:setup` only.
S3_CORS_ORIGINS=http://localhost:3000,http://localhost:3100

# --- Web (inlined into the client bundle at build time) -----------------------------------------
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Background jobs (used by: web sends jobs to the workers) ------------------------------------
WORKERS_URL=http://localhost:8081

# --- Ports for the Node services (optional; Cloud Run sets PORT itself) -------------------------
# SYNC_PORT=1234
# WORKERS_PORT=8081
